【修复】

1. 产品序号界面的相机图像中，存在一个用于确定OCR识别区域的roi红框，改红框可能不在图像中间。
经过排查，发现界面上的显示区域的分辨率为688*501（比例4:3），而设定的帧的尺寸为800*600，但是当相机不支持800*600的分辨率时，会使用默认的更小的分辨率，例如640*480。由于红框的位置时根据设定的分辨率（800*600）和红框的大小计算的，所以使用其他分辨率时，红框的位置可能部分图像中间。
解决方法：在打开相机的函数中，从相机类中读取相机的分辨率，根据分辨率和红框大小取计算红框位置。